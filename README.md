# DoBot Backend (Fly branch)

This branch contains only the backend (FastAPI) so it can be deployed independently (e.g. to Fly.io).

- Backend code lives in `backend/`
- Deploy from that directory (or point Fly to `backend/fly.toml`)
